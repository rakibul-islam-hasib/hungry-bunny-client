import React from 'react';
import { Helmet } from 'react-helmet-async';

const PendingMenu = () => {
    return (
        <div>
            <Helmet>
        <title>Hungry Bunny || admin || manage menu item</title>
      </Helmet>
            <h1>Pending menu</h1>
        </div>
    );
};

export default PendingMenu;