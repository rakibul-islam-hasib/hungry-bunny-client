import React from 'react';
import MenuCard from '../../components/Card/MenuCard';

const Menu = () => {
    return (
        <div>
            
            <MenuCard/>
        </div>
    );
};

export default Menu;